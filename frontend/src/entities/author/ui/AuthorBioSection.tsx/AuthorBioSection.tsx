import styles from './AuthorBioSection.module.scss'
import { useState } from 'react'

export const AuthorBioSection = () => {
  const [expanded, setExpanded] = useState(false)

  // 🧪 моканные данные
  const biography = `
Фёдор Михайлович Достоевский — один из величайших русских писателей,
мыслителей и философов XIX века. Его творчество исследует глубинные
психологические и нравственные конфликты человеческой души.

Наибольшую известность ему принесли романы «Преступление и наказание»,
«Идиот», «Братья Карамазовы» и «Бесы». Его произведения оказали огромное
влияние на мировую литературу и философию.
  `.trim()

  const shortText = biography.slice(0, 260) + '…'

  return (
    <section className={styles.bio}>
      <h2 className={styles.title}>Биография</h2>

      <p className={styles.text}>{expanded ? biography : shortText}</p>

      <button
        className={styles.toggle}
        onClick={() => setExpanded(prev => !prev)}
      >
        {expanded ? 'Свернуть' : 'Читать полностью'}
      </button>
    </section>
  )
}
